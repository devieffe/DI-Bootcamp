import React from 'react';
import './App';

class UserFavoriteAnimals extends React.Component {
    render() {
        return <p>favs</p>
            // user.map(item =>
            //     "<li>{user.favAnimals}</li>"
            // )
    }
}

export default UserFavoriteAnimals;